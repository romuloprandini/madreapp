<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="MadreApp.Pages.LoginPage"
             xmlns:controls="clr-namespace:MadreApp.Customs"
             xmlns:behaviors="clr-namespace:MadreApp.Behaviors"
             xmlns:converters="clr-namespace:MadreApp.Converters"
             IsBusy="{Binding IsBusy}">
    <ContentPage.Resources>
        <ResourceDictionary>
            <converters:BooleanInverter x:Key="BooleanInverter" />
        </ResourceDictionary>
    </ContentPage.Resources>
    <ContentPage.Content>
        <AbsoluteLayout>
            <StackLayout AbsoluteLayout.LayoutBounds="0,0,1,1"
                         AbsoluteLayout.LayoutFlags="All" >
                <Image Source="Icon"
                   HeightRequest="80"
                   WidthRequest="80" 
                   HorizontalOptions="Center"
                   VerticalOptions="Center" />
                <Label Text="Nome" />
                <Entry Placeholder="Nome" 
                       HorizontalOptions="FillAndExpand"
                       Text="{Binding Nome}"/>

                <Label Text="Telefone" />
                <StackLayout Orientation="Horizontal" 
                             HorizontalOptions="FillAndExpand">
                    <Label Text="+55" 
                           FontAttributes="Bold"
                           VerticalOptions="Center"/>
                    <Entry Placeholder="Telefone" 
                           Keyboard="Telephone"
                           HorizontalOptions="FillAndExpand"
                           Text="{Binding Telefone}">
                        <Entry.Behaviors>
                            <behaviors:MinLengthValidator x:Name="minLenghtValidator"
                                                          MinLength="10" />
                        </Entry.Behaviors>
                    </Entry>
                </StackLayout>
                <Label Text="O telefone deve ter no mínimo 10 números" 
                        TextColor="DarkRed" 
                        IsVisible="{Binding Source={x:Reference minLenghtValidator}, Path=IsValid, Converter={StaticResource BooleanInverter}}" />
                <Button Text="Entrar" 
                    Command="{Binding LoginCommand}"/>

                <BoxView Color="LightGray"
                     HeightRequest="2"
                     HorizontalOptions="FillAndExpand"
                     Margin="0,10,10,0" />

                <controls:FacebookLoginButton Text="Entrar pelo Facebook" 
                                          Command="{Binding FacebookLoginCommand}"
                                          OnSuccess="{Binding OnSucessFacebook}" 
                                          OnError="{Binding OnErrorFacebook}" 
                                          OnCancel="{Binding OnCancelFacebook}"
                                          BackgroundColor="#29487d"
                                          TextColor="White"/>
            </StackLayout>
            <BoxView IsVisible="{Binding IsBusy}" 
                     BackgroundColor="Black"
                     Opacity=".5"
                     AbsoluteLayout.LayoutBounds="0, 0, 1, 1"
                               AbsoluteLayout.LayoutFlags="All" />
            <ActivityIndicator IsVisible="{Binding IsBusy}" 
                               IsRunning="True"
                               AbsoluteLayout.LayoutBounds=".5, .5, 80, 80"
                               AbsoluteLayout.LayoutFlags="PositionProportional" />
        </AbsoluteLayout>
    </ContentPage.Content>
</ContentPage>